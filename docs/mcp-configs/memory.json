{
  "_description": "Cross-project persistent memory MCP server configurations",
  "examples": {
    "mcp-memory-libsql": {
      "command": "npx",
      "args": ["-y", "mcp-memory-libsql"],
      "env": {
        "LIBSQL_URL": "file:/Users/<user>/.config/claude-workspace/memory.db"
      }
    },
    "engram": {
      "_note": "Optional legacy provider. Requires: brew install gentleman-programming/tap/engram",
      "command": "engram",
      "args": ["mcp"]
    }
  },
  "setup_commands": {
    "mcp-memory-libsql": "claude-workspace memory configure --provider mcp-memory-libsql",
    "engram": "claude-workspace memory configure --provider engram"
  },
  "notes": {
    "mcp-memory-libsql": "Default provider. Auto-registered by claude-workspace setup. Runs via npx — no extra install beyond Node.js. libsql-backed knowledge graph. Data: ~/.config/claude-workspace/memory.db. Tools: mcp__mcp-memory-libsql__search_nodes, create_entities, read_graph, create_relations, delete_entity.",
    "engram": "Optional legacy provider. Go binary with FTS5 full-text search and SQLite persistence. Install: brew install gentleman-programming/tap/engram. Data: ~/.engram/engram.db. Switch: claude-workspace memory configure --provider engram."
  }
}
