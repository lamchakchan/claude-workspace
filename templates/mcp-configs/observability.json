{
  "_description": "Observability and monitoring MCP server configurations",

  "examples": {
    "sentry": {
      "type": "http",
      "url": "https://mcp.sentry.dev/mcp"
    },
    "grafana": {
      "type": "http",
      "url": "${GRAFANA_MCP_URL}"
    }
  },

  "setup_commands": {
    "sentry": "bun run cli/index.ts mcp remote https://mcp.sentry.dev/mcp --name sentry",
    "grafana": "bun run cli/index.ts mcp remote $GRAFANA_MCP_URL --name grafana"
  }
}
