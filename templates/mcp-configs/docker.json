{
  "_description": "Docker and Kubernetes MCP server configurations",
  "examples": {
    "docker": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-docker"
      ],
      "env": {}
    }
  },
  "setup_commands": {
    "docker": "claude-platform mcp add docker -- npx -y @modelcontextprotocol/server-docker"
  }
}
